--- magnolia-core/src/system/api.rs
+++ magnolia-core/src/system/api.rs
@@ -109,8 +109,8 @@
     let re = Regex::new(r"inet\s+(\d+\.\d+\.\d+\.\d+)").unwrap();
     let local_ip = re
         .captures(&ip_stdout)
-        .and_then(|cap| cap.get(1))
-        .map_or("Unknown".to_string(), |m| m.as_str().to_string());
+        .and_then(|cap: regex::Captures| cap.get(1))
+        .map_or("Unknown".to_string(), |m: regex::Match| m.as_str().to_string());

     Ok(NetworkInfo {
         active_ssid,
@@ -168,7 +168,7 @@
 pub async fn generate_recovery_key() -> Result<String, String> {
     // Generate a secure bip39 recovery phrase
     use bip39::{Language, Mnemonic};
-    let mut rng = rand::thread_rng();
+    let mut rng = bip39::rand::thread_rng();
     let mnemonic =
         Mnemonic::generate_in_with(&mut rng, Language::English, 24).map_err(|e| e.to_string())?;
