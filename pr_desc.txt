🎯 **What:** The testing gap addressed
Tested the `list_directory` method in `magnolia-core/src/handlers/files.rs` which was previously lacking coverage.

📊 **Coverage:** What scenarios are now tested
- The happy path (correct execution, properly returning directories before files, properly sorted).
- The error condition path (properly failing and surfacing an error string when an invalid path is provided).

✨ **Result:** The improvement in test coverage
The code functionality of iterating directories and organizing files vs directories is now safely tested, allowing developers to refactor with confidence without breaking the application logic around directory traversal or expected output format.
